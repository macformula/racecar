name: Test Self-Hosted Runner

on:
  push:
  workflow_dispatch:

jobs:
    test-self-hosted:
      runs-on: self-hosted

      steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        run: |
          cd firmware
          make PROJECT=Demo/Blink PLATFORM=cli build