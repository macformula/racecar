name: Build Projects

on:
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build DemoProject
      run: make PLATFORM=stm32f767 PROJECT=DemoProject
